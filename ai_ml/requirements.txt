# AI/ML detection layer dependencies.
#
# torch is CPU-only here; install from https://download.pytorch.org/whl/cpu
# for the same build, or let pip resolve a default build on a GPU machine.
#
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r ai_ml/requirements.txt
#   python -m spacy download en_core_web_sm

requests>=2.31
transformers>=4.44
datasets>=2.20
accelerate>=1.1.0
scikit-learn>=1.4
pandas>=2.2
matplotlib>=3.8
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
